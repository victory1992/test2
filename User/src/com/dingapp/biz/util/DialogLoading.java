package com.dingapp.biz.util;

import android.app.Dialog;
import android.content.Context;

public class DialogLoading {
	/**
	 * loading
	 */
	public static Dialog getDialog(Context context, Dialog dialog) {
		// View view = View
		// .inflate(context, R.layout.loading, null);
		// ImageView iv_hulu_progress = (ImageView)
		// view.findViewById(R.id.iv_hulu_progress);
		// AnimationDrawable animation = (AnimationDrawable)
		// iv_hulu_progress.getDrawable();
		// animation.start();
		// dialog.setContentView(view);
		// Window window = dialog.getWindow();
		// window.setBackgroundDrawable(new BitmapDrawable());
		// window.setGravity(Gravity.CENTER);
		// LayoutParams params = window.getAttributes();
		// params.height = android.view.ViewGroup.LayoutParams.WRAP_CONTENT;
		// params.width = android.view.ViewGroup.LayoutParams.WRAP_CONTENT;
		return dialog;
	}
}
